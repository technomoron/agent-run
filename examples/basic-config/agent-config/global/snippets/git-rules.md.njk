## Git Rules

Do not commit, tag, or push unless the user explicitly asks for that action.

Guardrails:

- blockGitWrite: `{{ guardrails.blockGitWrite }}`
- blockPublish: `{{ guardrails.blockPublish }}`
- blockGithubRelease: `{{ guardrails.blockGithubRelease }}`
