# Starter AGENTS for {{ profile }}

Do not edit this generated file. Edit `agent-run.jsonc` and `local.md.njk`.

{% for section in renderedAgentSections %}
{{ section }}

{% endfor %}
## Paths

- projectRoot: `{{ projectRoot }}`
- agentDir: `{{ agentDir }}`
- reviewFile: `{{ paths.reviewFile }}`
- memoriesDir: `{{ paths.memoriesDir }}`

## Skills

{% for skill in skills %}
- `{{ skill.name }}`: {{ skill.description }}
{% endfor %}
