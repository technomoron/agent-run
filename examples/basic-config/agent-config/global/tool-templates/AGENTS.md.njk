# Starter AGENTS for {{ profile }}

Do not edit this generated file. Edit root defaults or profile-specific source files.

{% for section in renderedAgentSections %}
{{ section }}

{% endfor %}
## Paths

- projectRoot: `{{ projectRoot }}`
- agentDir: `{{ agentDir }}`
- profileDir: `{{ profileDir }}`
- reviewFile: `{{ paths.reviewFile }}`
- reviewConsolidatedFile: `{{ paths.reviewConsolidatedFile }}`
- memoriesDir: `{{ paths.memoriesDir }}`
- globalMemoryDir: `{{ paths.globalMemoryDir }}`

## Skills

{% for skill in skills %}
- `{{ skill.name }}`: {{ skill.description }}
{% endfor %}
