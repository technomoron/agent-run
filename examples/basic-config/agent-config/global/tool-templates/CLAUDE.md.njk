# Starter CLAUDE for {{ profile }}

{% for section in renderedAgentSections %}
{{ section }}

{% endfor %}
Claude reads this file from the private agent directory.

projectMemoryDir: `{{ paths.projectMemoryDir }}`
globalMemoryDir: `{{ paths.globalMemoryDir }}`
