## Local Project Instructions

This starter profile belongs to `{{ profile }}`.

Use the project root at `{{ projectRoot }}` and keep generated files in `{{ agentDir }}`.
Keep source profile edits in `{{ profileDir }}`.

When brain is enabled, ordinary questions need no lookup unless the answer needs
saved project knowledge. Reuse loaded context and skills. Create saved tasks only
when the user asks to track them or explicitly authorizes a task-tracking workflow.
